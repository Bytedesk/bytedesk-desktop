// import { Layout } from "antd"
import { Outlet } from "react-router-dom";

// const { Content } = Layout

const Auth = () => {
  return (
    <>
      <Outlet />
    </>
  );
};

export default Auth;
